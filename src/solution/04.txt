
About.jsx
*********

import { useState } from "react";

export default function Notifications() {


   const [notifications, setNotifications] = useState(true);


   return (
      <div>

         <p>
            {notifications
               ? "Értesítések be vannak kapcsolva" : "Értesítések ki vannak kapcsolva"}
         </p>

         <button
            onClick={() =>
               setNotifications(!notifications)
            }
         >

            {notifications
               ? "Kikapcsolás" : "Bekapcsolás"}
         </button>

      </div>
   );
}

App.css (About.jsx)
*******************

body {
  margin: 0;
  font-family: Arial, sans-serif;
  background-color: #f0f0f0;
}

/* 2. TARTALOM DOBOZ */
div {
  width: 350px;
  margin: 80px auto; 
  padding: 20px;
  background-color: white;
  border-radius: 10px;
  text-align: center;
}

/* 3. SZÖVEG */
p {
  font-size: 16px;
  margin-bottom: 20px;
}

/* 4. GOMB */
button {
  padding: 10px 20px;
  background-color: blue;
  color: white;
  border: none;
  border-radius: 5px;
  cursor: pointer;
}

/* 5. GOMB HOVER */
button:hover {
  background-color: darkblue;
}

